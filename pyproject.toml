# Ruff configuration for JM-MusicAnalyzer
# See: https://docs.astral.sh/ruff/

[tool.ruff]
exclude = [
    "backups",
    "dist",
    "build",
    "__pycache__",
]

[tool.ruff.lint]
# Select rule sets
select = ["E", "F", "W", "I", "N", "UP", "YTT", "BLE", "B", "A", "C4", "DTZ", "T10", "EM", "EXE", "ISC", "ICN", "G", "INP", "PTH", "PT", "Q", "RSE", "RET", "SLF", "SLOT", "SIM", "TID", "TCH", "INT", "ARG", "PIE", "PL", "TRY", "FLY", "PERF", "RUF"]
ignore = [
    "E501",  # Line too long (we handle manually)
    "B008",  # Do not perform function calls in argument defaults
    "C901",  # Too complex
    "G004",  # Logging f-string — project uses f-strings intentionally (331 hits)
    "PTH",  # os.path vs pathlib — migration optional, 400+ hits (PTH100..PTH123)
    "N802",  # Qt overrides must stay camelCase (paintEvent, setVolume)
    "N803",  # Argument names (logicalIndex from Qt)
    "N806",  # Variable in function (Qt)
    "N815",  # mixedCase variable in class scope (Qt signals)
    "PLC0415",  # import outside top-level — lazy imports for startup/circular
]
fixable = ["ALL"]
unfixable = []

[tool.ruff.format]
# Use double quotes for strings
quote-style = "double"
# Indent with 4 spaces
indent-style = "space"
line-ending = "auto"

[tool.ruff.lint.isort]
known-first-party = ["JM_MusicAnalizer", "ui", "core", "services", "workers", "config"]
force-sort-within-sections = true
force-wrap-aliases = true
split-on-trailing-comma = true
